# Copy this file to .env (never commit .env)

# Base URL of the Django API. "/api" uses the Vite dev proxy (see vite.config.js).
VITE_API_BASE_URL=/api

# true  = every screen runs on mock data (no backend needed)
# false = the app calls the Django API
VITE_USE_MOCK_DATA=true
